{
  "private": true,
  "description": "Nefor — multi-agent DAG orchestrator for Pi",
  "dependencies": {
    "yaml": "^2.7.1"
  },
  "devDependencies": {
    "vitest": "^3.1.1"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  }
}
