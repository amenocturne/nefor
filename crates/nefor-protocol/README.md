# nefor-protocol

Types and JSON Lines codec for the [Nefor Composition Protocol (NCP) v0.1](../../protocol/v0.1/spec.md). Engine and plugin implementations depend on this crate to produce and consume NCP envelopes without reimplementing the wire contract.
