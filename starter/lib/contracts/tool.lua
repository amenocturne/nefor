-- starter/lib/contracts/tool.lua — tool-gate wire-protocol contract.
--
-- Phase 1 placeholder — populated in Phase 2 once the
-- agentic_workflow refactor extracts tool-side wire helpers
-- (tool.invoke / tool.result correlation, generic-tool type names,
-- spawn_graph virtual-source plumbing).

local M = {}

return M
