-- starter/lib/contracts/provider.lua — provider wire-protocol contract.
--
-- Phase 1 placeholder — populated in Phase 2 once the
-- agentic_workflow refactor extracts provider-side wire helpers
-- (envelope kinds, chat.* translation, stream gating, pending-entry
-- shapes). See tmp/refactor-plan/wire_protocol_spec.md for the target.

local M = {}

return M
