# starter/

Reference `init.lua` for getting a working nefor with the monorepo's plugins wired up.

**This is not auto-installed.** Read it, copy what you want into your own `~/.config/nefor/init.lua`, adapt to taste.
